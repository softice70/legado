# Legado 构建问题解决方案

## 常见构建错误及解决方案

### 1. Firebase 依赖下载失败

**错误信息**:
```
Could not find firebase-encoders-json-18.0.1.aar (com.google.firebase:firebase-encoders-json:18.0.1).
Searched in the following locations:
    https://maven.aliyun.com/repository/google/com/google/firebase/firebase-encoders-json/18.0.1/firebase-encoders-json-18.0.1.aar
```

**原因**: Firebase相关依赖在国内镜像源中可能不完整或版本不一致。

**解决方案**:
1. 已调整仓库优先级，Google官方仓库优先
2. 在阿里云镜像中排除Firebase相关包
3. 如果仍有问题，可以临时禁用国内镜像

**临时解决方法**:
```bash
# 重命名init.gradle文件暂时禁用全局镜像配置
mv init.gradle init.gradle.bak

# 清理缓存重新构建
./gradlew clean --refresh-dependencies
./gradlew build
```

### 2. Kotlin 版本警告

**警告信息**:
```
Deprecated Gradle Property 'kotlin.incremental.useClasspathSnapshot' Used
```

**解决方案**: 已在gradle.properties中注释掉此废弃配置。

### 3. 代理配置错误

**错误信息**:
```
Invalid https.proxyPort '', using default 443
```

**解决方案**: 已在gradle.properties中注释掉空的代理配置。

### 4. 网络连接超时

**解决方案**:
1. 检查网络连接
2. 使用VPN或代理
3. 切换到移动网络尝试
4. 增加Gradle超时时间：
```properties
# 在gradle.properties中添加
org.gradle.internal.http.connectionTimeout=120000
org.gradle.internal.http.socketTimeout=120000
```

## 推荐的构建流程

### 步骤1: 环境检查
```bash
# Windows
check-environment.bat

# Linux/macOS  
./check-environment.sh
```

### 步骤2: 清理缓存
```bash
./gradlew clean --refresh-dependencies
```

### 步骤3: 构建项目
```bash
# 普通构建
./gradlew build

# 详细日志构建（用于调试）
./gradlew build --info --stacktrace
```

### 步骤4: 如果构建失败
```bash
# Windows
fix-build.bat

# Linux/macOS
./fix-build.sh
```

## 网络环境优化建议

### 1. 企业网络环境
如果在企业网络环境中，可能需要配置代理：
```properties
# gradle.properties
systemProp.http.proxyHost=your-proxy-host
systemProp.http.proxyPort=your-proxy-port
systemProp.https.proxyHost=your-proxy-host
systemProp.https.proxyPort=your-proxy-port
```

### 2. 家庭网络环境
- 使用国内镜像源（已配置）
- 如果DNS解析慢，可以配置公共DNS（如8.8.8.8）
- 考虑使用科学上网工具

### 3. 移动网络环境
- 注意流量消耗
- 首次构建可能需要下载大量依赖
- 建议在WiFi环境下进行首次构建

## 常用调试命令

```bash
# 查看所有依赖
./gradlew dependencies

# 查看依赖冲突
./gradlew dependencyInsight --dependency firebase-encoders-json

# 查看Gradle版本信息
./gradlew --version

# 查看项目信息
./gradlew projects

# 强制刷新所有依赖
./gradlew build --refresh-dependencies --recompile-scripts

# 清理所有缓存
./gradlew clean
rm -rf ~/.gradle/caches  # Linux/macOS
rmdir /s /q %USERPROFILE%\.gradle\caches  # Windows
```

## 联系支持

如果以上方案都无法解决问题：
1. 查看详细错误日志
2. 检查网络环境
3. 尝试在不同时间段构建
4. 考虑使用官方源（注释掉镜像源配置）