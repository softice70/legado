# Legado 完整构建解决方案

## 问题分析

根据之前的构建失败情况，主要问题可能包括：
1. Firebase依赖下载失败 ✅ (已解决)
2. Cronet库下载失败 (Google存储服务在国内访问困难)
3. 其他网络相关的依赖问题

## 解决方案

### 方案1: 使用诊断脚本 (推荐)
```cmd
diagnose-build.bat
```
这个脚本会：
- 检查Gradle状态
- 清理项目
- 检查依赖解析
- 尝试构建并生成详细日志
- 显示具体错误信息

### 方案2: 测试跳过Cronet下载
```cmd
build-without-cronet.bat
```
这个脚本会：
- 临时禁用Cronet下载
- 测试基本构建是否成功
- 自动恢复配置

### 方案3: 手动排查步骤

#### 步骤1: 清理所有缓存
```cmd
# 清理Gradle缓存
./gradlew clean

# 清理本地缓存
rmdir /s /q .gradle
rmdir /s /q build
rmdir /s /q app\build
```

#### 步骤2: 检查网络连接
```cmd
# 测试Google服务连接
ping storage.googleapis.com

# 测试镜像源连接
ping mirrors.cloud.tencent.com
```

#### 步骤3: 分步构建
```cmd
# 只编译不打包
./gradlew compileDebugKotlin

# 处理资源
./gradlew processDebugResources

# 完整构建
./gradlew assembleDebug
```

## 常见错误及解决方案

### 1. Cronet下载失败
**错误特征**: 
- 提示无法连接到 storage.googleapis.com
- Cronet相关的下载任务失败

**解决方案**:
- ✅ 已修改为使用腾讯云镜像源
- 如仍失败，可临时跳过Cronet下载

### 2. 依赖版本冲突
**解决方案**:
```cmd
# 查看依赖树
./gradlew dependencies

# 强制刷新依赖
./gradlew build --refresh-dependencies
```

### 3. 内存不足
**解决方案**:
在 `gradle.properties` 中增加内存配置：
```properties
org.gradle.jvmargs=-Xmx8g -XX:MaxMetaspaceSize=1g
```

### 4. 网络超时
**解决方案**:
```properties
# 在gradle.properties中添加
org.gradle.internal.http.connectionTimeout=300000
org.gradle.internal.http.socketTimeout=300000
```

## 构建环境要求

### 必需环境
- ✅ JDK 17+ (已确认)
- ✅ Android SDK (已确认)
- ✅ Gradle 8.11.1 (已确认)

### 网络要求
- 稳定的网络连接
- 能够访问Maven仓库
- 如需Cronet功能，需要能访问Chromium下载源

## 调试命令集合

```cmd
# 环境检查
check-environment.bat

# 构建诊断
diagnose-build.bat

# 跳过Cronet测试
build-without-cronet.bat

# 手动构建步骤
./gradlew clean
./gradlew build --info --stacktrace

# 依赖分析
./gradlew dependencies --configuration debugCompileClasspath

# 查看构建任务
./gradlew tasks --all
```

## 成功构建的标志

构建成功后，应该能在以下位置找到APK文件：
```
app/build/outputs/apk/app/debug/legado_app_[version].apk
```

## 如果所有方案都失败

1. **检查防火墙/代理设置**
2. **尝试使用移动网络**
3. **更换DNS服务器** (如8.8.8.8)
4. **使用VPN或科学上网工具**
5. **在不同时间段尝试** (避开网络高峰期)

## 联系支持

如果问题仍然存在，请提供：
1. 完整的错误日志 (build.log)
2. 网络环境信息
3. 系统配置信息
4. 尝试过的解决方案

---
**更新时间**: 2025年1月11日