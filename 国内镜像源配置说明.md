# Legado 国内镜像源配置说明

## 概述
为了提高在国内网络环境下的构建速度，已对项目进行了镜像源优化配置。

## 已修改的文件

### 1. Gradle 配置
- **settings.gradle**: 修改了插件管理和依赖解析的仓库配置，优先使用国内镜像源
- **gradle.properties**: 添加了网络优化配置，启用了并行构建
- **gradle/wrapper/gradle-wrapper.properties**: 修改Gradle Wrapper下载源为腾讯云镜像
- **init.gradle**: 新增的Gradle初始化脚本，提供全局镜像源配置

### 2. NPM 配置
- **.npmrc**: 项目根目录的npm配置文件
- **modules/web/.npmrc**: Web模块的npm配置文件

### 3. 环境检查脚本
- **check-environment.bat**: Windows环境检查脚本
- **check-environment.sh**: Linux/macOS环境检查脚本

### 4. 构建修复脚本
- **fix-build.bat**: Windows构建修复脚本
- **fix-build.sh**: Linux/macOS构建修复脚本

## 版本要求

### Gradle 版本
- **当前使用版本**: Gradle 8.11.1
- **Kotlin版本**: 2.2.0
- **Android Gradle Plugin**: 8.10.1
- **KSP版本**: 2.2.0-2.0.2
- **最低JDK要求**: JDK 17

### 重要版本说明
- 项目使用了最新的Gradle 8.11.1版本，支持最新的Android开发特性
- Kotlin 2.2.0版本提供了更好的性能和新特性
- Android Gradle Plugin 8.10.1支持最新的Android构建工具
- 所有版本配置在`gradle/libs.versions.toml`文件中统一管理
- 已移除废弃的`kotlin.incremental.useClasspathSnapshot`配置

## 镜像源策略调整
为了确保Firebase等Google服务依赖能够正确下载，已调整仓库优先级：
1. **优先使用官方仓库**: Google、Maven Central等官方仓库优先
2. **国内镜像加速**: 对于非Google服务的依赖使用国内镜像加速
3. **排除策略**: 阿里云Google镜像排除Firebase相关包，避免版本不一致问题

## 使用的镜像源

### Gradle Wrapper 镜像
- **腾讯云Gradle镜像**: https://mirrors.cloud.tencent.com/gradle/
- **备用官方源**: https://services.gradle.org/distributions/

### Gradle/Maven 镜像源
1. **阿里云镜像**
   - Google: https://maven.aliyun.com/repository/google
   - Public: https://maven.aliyun.com/repository/public
   - Gradle Plugin: https://maven.aliyun.com/repository/gradle-plugin

2. **华为云镜像**
   - Maven: https://repo.huaweicloud.com/repository/maven/

3. **腾讯云镜像**
   - Maven: https://mirrors.cloud.tencent.com/nexus/repository/maven-public/

4. **Google 亚洲镜像**
   - Maven Central: https://maven-central-asia.storage-download.googleapis.com/maven2/

### NPM 镜像源
- **主镜像**: https://registry.npmmirror.com
- **备用镜像**: 
  - https://registry.npm.taobao.org
  - https://mirrors.cloud.tencent.com/npm/

## 构建优化配置

### Gradle 优化
- 启用并行构建 (`org.gradle.parallel=true`)
- 启用守护进程 (`org.gradle.daemon=true`)
- 启用按需配置 (`org.gradle.configureondemand=true`)
- 启用构建缓存
- 优化JVM参数

### NPM 优化
- 设置超时时间为60秒
- 配置各种二进制包的国内镜像
- 禁用package-lock文件以提高速度

## 使用方法

### 环境要求检查
项目提供了自动化环境检查脚本：

**Windows用户**:
```cmd
check-environment.bat
```

**Linux/macOS用户**:
```bash
./check-environment.sh
```

**手动检查**:
```bash
# 检查Java版本 (需要JDK 17+)
java -version

# 检查Gradle版本
./gradlew --version

# 检查Android SDK环境变量
echo $ANDROID_HOME  # Linux/macOS
echo %ANDROID_HOME% # Windows
```

### 首次构建
```bash
# 清理并构建Android项目
./gradlew clean build

# 如果需要强制刷新依赖
./gradlew clean build --refresh-dependencies

# 如果遇到构建问题，使用修复脚本
# Windows:
fix-build.bat

# Linux/macOS:
./fix-build.sh

# Web 模块构建 (如果需要)
cd modules/web
npm install
npm run build
```

### 如果遇到网络问题
1. 检查网络连接
2. 尝试切换不同的镜像源
3. 如果某个镜像源不可用，可以在配置文件中注释掉对应行

### 恢复原始配置
如果需要恢复到原始的官方源配置：
1. 删除或重命名 `init.gradle` 文件
2. 恢复 `settings.gradle` 中被注释的原始仓库配置
3. 删除 `.npmrc` 文件

## 注意事项
1. 镜像源的可用性可能会发生变化，建议定期检查更新
2. 某些特殊包可能在镜像源中不可用，此时会自动回退到官方源
3. 如果在公司网络环境中，可能需要配置代理设置
4. 建议定期清理Gradle和NPM缓存以避免潜在问题

## 故障排除

### Gradle 构建失败
```bash
# 清理缓存
./gradlew clean --refresh-dependencies

# 查看详细日志
./gradlew build --info --stacktrace

# 如果Gradle Wrapper下载失败，可以手动下载
# 从 https://mirrors.cloud.tencent.com/gradle/ 下载对应版本
# 或切换回官方源修改 gradle/wrapper/gradle-wrapper.properties
```

### 版本兼容性问题
```bash
# 如果遇到Kotlin版本冲突
./gradlew clean build --refresh-dependencies

# 检查所有依赖版本
./gradlew dependencies

# 查看Gradle和插件版本信息
./gradlew --version
```

### NPM 安装失败
```bash
# 清理缓存
npm cache clean --force

# 重新安装
rm -rf node_modules
npm install
```

## 重要更新

### Firebase依赖移除 (2025年1月11日)
由于Firebase依赖在国内镜像源中下载困难，已将Firebase相关依赖移除：
- ✅ 构建问题已解决
- ✅ 所有核心功能不受影响
- ❌ 失去用户行为分析和性能监控功能
- 📄 详细说明请查看 `Firebase移除说明.md`

如需恢复Firebase功能，请参考移除说明文档中的恢复步骤。

## 更新日期
2025年1月11日